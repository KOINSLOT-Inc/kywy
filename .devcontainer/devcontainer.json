{
	"image": "mcr.microsoft.com/devcontainers/universal:2",
	// Removed features to avoid build-time feature image layers that can inflate disk usage.
	// If you need the `make` feature, consider enabling it locally or adding it to a prebuilt image.
	"features": {},
	"postCreateCommand": "bash .devcontainer/dev-container-setup.sh --skip-make"
}